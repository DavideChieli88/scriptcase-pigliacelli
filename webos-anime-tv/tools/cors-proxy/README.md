# Personal use only. Do not expose publicly.

## Dev (browser)
`npm run dev` already mounts `/proxy` inside Vite — **non serve** un secondo terminale.

## TV / LAN
Avvia questo server sul PC e in Impostazioni app punta a `http://<IP-PC>:8787`:

```bash
npm run proxy
```
