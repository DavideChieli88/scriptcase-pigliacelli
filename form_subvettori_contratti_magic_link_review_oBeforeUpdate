/**
 * form_subvettori_contratti_for_subvectors_magic_link_review
 * → onBeforeUpdate E onBeforeInsert: lasciare VUOTO (o solo questo commento).
 *
 * NON usare sc_exec_sql / sc_lookup qui: Scriptcase apre già una transazione PDO
 * sul salvataggio e si ottiene:
 *   "There is already an active transaction"
 *
 * La sincronizzazione anagrafica + notifica operatori va in onAfterUpdate / onAfterInsert:
 * incollare il contenuto di form_subvettori_contratti_magic_link_review_onAfterSave.
 */
